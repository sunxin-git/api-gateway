-- ============================================================================
-- Migration 0010 DOWN: 回滚 TOS 结果对象元数据
-- ----------------------------------------------------------------------------
-- 删 oss_object_meta（IF EXISTS 幂等，与 0005 down 风格一致）。TOS 上已存对象不由本回滚清理。
-- ============================================================================

DROP INDEX IF EXISTS idx_task_settled_needing_store;
DROP TABLE IF EXISTS oss_object_meta;
